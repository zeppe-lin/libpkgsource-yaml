# libpkgsource-yaml

Strict native YAML syntax adapter extracted from `libpkgsource` 2.1.0.

This import commit preserves the original parser API and format contracts as a
reviewable provenance boundary. Subsequent commits establish the independent
repository's first public API and release contract.
